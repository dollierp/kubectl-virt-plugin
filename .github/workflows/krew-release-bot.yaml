name: krew-release-bot
on:
  workflow_dispatch: {}
jobs:
  run-krew-release-bot:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Update new version in krew-index
      uses: rajatjindal/krew-release-bot@v0.0.38
      with:
        krew_template_file: /dev/null
